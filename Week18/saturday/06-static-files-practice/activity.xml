make a database called hangouts_in_the_bay_db

make a table called places
	the table should have an id and a place_name column

insert 4 records into the places table

make an express app with a public folder that is static with an index.html file

make a route /places

	that sends all the places from the hangouts_in_the_bay_db as json

in the index.html file make an ajax call to the /places route and make paragraph tags for each place and put them in the body of the index.html file